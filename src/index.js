import './css/styles.css';

const DEBOUNCE_DELAY = 300;

function fetchCountries(name) {
  fetch(
    'https://jsonplaceholder.https://restcountries.com/v3.1/name/peru.com/users'
  ).then(responcs => response.json());
}
